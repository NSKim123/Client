
#include <stdio.h>

/*

	포인터 변수
	: '주소'를 '값'으로 가지는 '변수'



*/
//int tA = 0;    정수 타입의 '값'을 가지는 '변수'

int main()
{
	int tA = 777;

	int* tpA = NULL;    //여기서 * 기호는 포인터 변수를 선언하는 연산자이다.
	//포인터 변수는 널 로 초기화한다.

	return 0;
}